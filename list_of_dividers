На вход программе подается натуральное число. Напишите программу, которая создает список состоящий из делителей введенного числа.

n = int(input())
divs = []
for i in range(1, n + 1):
    if n % i == 0:
        divs.append(i)
print(divs)

# also

def get_factors(num):
    divs = [i for i in range(1, num + 1) if not num % i]
    return divs
# считываем данные
n = int(input())
